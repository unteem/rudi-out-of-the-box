/.project

# Sensitive configuration files
.passwords.env
.env.local
.env.smtp
.keystore-info.txt
*.producer-credentials.env
register-on-portal.sql

# Generated SQL files (templates are kept in git)
config/rudi-init/01-usr.sql
!config/rudi-init/01-usr.sql.template

# Generated properties files (templates are kept in git)
config/*/*.properties
!config/*/*.properties.template

# Backup directories
config-backup-*

# SSL certificates and keys (add your actual certs to separate secure storage)
certs/*.pem
certs/*.key
certs/*.crt
config/*/key/id_rsa
config/*/key/id_rsa.pub
config/*/*.jks
config/*/*.p12

# Database data (should be backed up separately)
database-data/
data/dataverse/dataverse-files/
data/magnolia/repository/
data/solr/solr-data/

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db
*~
.swp

# IDE
.idea/
.vscode/
*.iml
